function asientosDisponibles(disponibles, AOcupar){
    let verdad = "mensajepositivo"
    let falso = "mensajenegativo"
    for (let i =0; i<disponibles.length; i++) {
        if (disponibles[i]=AOcupar)
        return -
        else if (disponibles[i]!=AOcupar)
   
}
    return 
console.log(asientosDisponibles([1,2,3],(2)))